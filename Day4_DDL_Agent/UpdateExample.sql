/* Update Command */

Used to update the table records, 

Update table_name set field1=value, field2=value {where condition} 

select * from Agent;

Update Agent set FullName = FirstName + ' ' + MI + ' ' + LastName
GO

